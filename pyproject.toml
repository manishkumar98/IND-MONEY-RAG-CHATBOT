[project]
name = "ind-money-rag-chatbot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic>=2.0",
    "pydantic-settings",
    "chromadb>=0.6.3",
    "groq",
    "python-dotenv",
    "requests",
]
